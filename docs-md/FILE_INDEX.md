# FILE_INDEX.md — Bonus Game Repository File Index

> Generated by documentation pass. All paths relative to repo root.
> Source evidence: full recursive directory listing + file reads.

---

## Root-Level Files

| File | Role |
|------|------|
| `index.html` | **Browser entry point.** Single-page PWA shell. Contains embedded `HebrewValidator` class (lines 2802–3145 legacy), script loaders for Firebase SDK and spine modules, build version meta tags. |
| `sw.js` | **Service Worker.** Handles OneSignal push routing, offline cache with auto-invalidating cache name (`boost-{timestamp}`), and notification-click postMessage routing to app. |
| `styles.css` | **All UI styles** (~90 KB). Dark navy gradient design system with CSS custom properties. Mobile-first, portrait orientation. RTL Hebrew layout. |
| `manifest.json` | PWA metadata: app name, icons, `orientation: portrait`, `dir: rtl`. |
| `twa-manifest.json` | Trusted Web Activity config for Android app wrapper. |
| `firebase.json` | Firebase hosting config + emulator config (database port 9000, single-project mode). |
| `firebase.database.rules.json` | Firebase Realtime Database security rules (7,918 bytes). |
| `.firebaserc` | Firebase project alias file. |
| `config.example.js` | Template for `config.js` (gitignored). Documents required fields: `onesignalAppId`, `onesignalKey`, music URL. |
| `config.js` | **GITIGNORED.** Actual credentials. Never commit. |
| `package.json` | Build scripts: `test:unit`, `test:emulator`, `test:e2e`, `dict:add`, `dict:export`. Dev-only (no runtime `node_modules`). |
| `playwright.config.js` | Playwright E2E config: base URL `http://127.0.0.1:4173`, Python HTTP server, 30s timeout. |
| `privacy-policy.html` | Legal disclosure page. |
| `README.md` | Getting-started guide. |
| `SPINE_TODO.md` | Active cutover checklist tracking legacy → spine migration. 609 unit tests passing. |
| `tmp.json` | Temporary scratch file (not committed to production). |

---

## `/src/` — ES6 Module Source Tree

### `src/main.js`
**Primary application entry point.** Boots entire spine: dictionary, Firebase, OneSignal, event bus, all sessions/controllers/screens. Exposes `globalThis.__spine` for dev/test access.

### `src/events/`
| File | Role |
|------|------|
| `bus.js` | Central pub/sub event dispatcher. |
| `commands.js` | `CMD.*` command constant definitions. |
| `eventTypes.js` | `EV.*` event type enums. |

### `src/game/core/` — Pure Game Engine
| File | Role |
|------|------|
| `gameEngine.js` | State machine. `createInitialState()`, `createEngine()`. Handles all command dispatching. Pure JS, no DOM/Firebase. |
| `board.js` | Board representation. `BOARD_SIZE=10`. Main 10×10 grid + 12 off-grid bonus squares. |
| `tileBag.js` | Tile bag. `RACK_SIZE=8`. Seeded shuffle via `createBag(seed)`, `drawInto()`, `returnTilesAndShuffle()`. |
| `letterDistribution.js` | Hebrew letter values (`HV`) and bag counts (`HD`). 108 total tiles, 2 jokers. |
| `moveValidator.js` | `validateMove()`: collinearity, gap, connectivity, first-move rules. |
| `scoringEngine.js` | `scoreMove()`, `getAllWords()`. `BINGO_BONUS=50`. |
| `turnManager.js` | `applyMove()`, `applyPass()`, `applyExchange()`, `applyLock()`, `applyResign()`, `isGameOver()`. `LEGACY_PASS_GAME_OVER_THRESHOLD=6`. |
| `hebrewDictionary.js` | `isValid()`, `loadDict()`. Loads from `data/dictionary.base.txt`. Complex lemmatization + prefix/suffix stripping. |
| `*.test.js` | Inline unit tests for each module. |

### `src/game/boosts/` — Bonus System
| File | Role |
|------|------|
| `index.js` | Plugin registry. `registerAllBoosts()` called at boot. Exports `BONUS_TILE_DEFS`, `WHEEL_OUTCOMES`, `BONUS_TYPES`, `BDEFS`. |
| `bonusTileDefs.js` | B1–B13 bonus square definitions (type, points, trigger kind). |
| `bonusResolver.js` | `resolveBonusActivation()`, `resolveMiniGameResult()`, `resolveWheelResult()`. Pure, no side effects. |
| `data.js` | `BONUS_TYPES` array (15 shuffled assignments), `BDEFS` board slot positions. |
| `boostEngine.js` | Hook system: `runHook()`, `TRIGGERS`. Consumed by gameEngine. |
| `futureEffects/multiplyNextTurns.js` | 2× or 4× score multiplier for N turns. |
| `futureEffects/extraTurn.js` | Extra turn effect (`repeatTurn: true`). |
| `futureEffects/timerBonus.js` | Add time to turn deadline. |
| `futureEffects/skipOpponentTurn.js` | Skip opponent's next turn. |
| `futureEffects/freeTileSwap.js` | Free tile exchange (no turn loss). |
| `futureEffects/cancelNextOpponentBonus.js` | Block opponent's next bonus activation. |
| `futureEffects/autoExtraScore.js` | Immediate point bonus (deferred UI acknowledge). |
| `futureEffects/plugins.test.js` | Boost integration tests. |

### `src/game/sessions/` — Session Modes
| File | Role |
|------|------|
| `modes.js` | `modeDescriptor()`, `MODES` object. Defines 7 modes with online/timer/push/expiry attributes. |
| `localGameSession.js` | `createLocalGameSession()`. Thin wrapper around engine for offline play. |
| `botGameSession.js` | `attachBotPlayer()`. Bot with pause/resume around bonus flows. |
| `tutorialSession.js` | Scripted tutorial opponent. `TUTORIAL_WORD='שלום'`, 4 preset bot moves. |
| `onlineGameSession.js` | `createOnlineGameSession()`. Firebase-backed session with version-guarded transactions. |
| `botSearch.js` | Bot move search algorithm. |
| `*.test.js` | Session unit tests. |

### `src/game/online/` — Firebase / Network Layer
| File | Role |
|------|------|
| `schema.js` | PATH/FIELD/STATUS constants. Serialization: `serializeBoard()`, `deserializeBoard()`, `serializeBonusBoard()`. |
| `firebaseClient.js` | Firebase SDK loader (v10.13.0). `ensureApp()`, `loadFirebaseSDK()`. Test injection via `setFirebaseImplForTests()`. |
| `roomService.js` | Room CRUD + `commitTransaction()` with version guard. Turn timer helpers. |
| `inviteService.js` | `sendInvite()`, `acceptInvite()`, `rejectInvite()`. Live=5min TTL, Async=7d TTL. |
| `matchmakingService.js` | `joinQueue()`, `tryPair()`. Rating-range + strict-search compatibility. |
| `asyncSessionService.js` | `listAsyncSessions()`, `watchAsyncSessions()`, `dismissForUid()`. |
| `asyncReminderService.js` | `classify()`, `sweepForUser()`. 24h idle → reminder, 7d idle → expire. |
| `presenceService.js` | `startPresence()` (10s heartbeat), `watchPresence()`. |
| `roomCodeService.js` | 6-digit room codes, 30min TTL, atomic `claimByCode()`. |
| `timeoutWatchdog.js` | Opponent-side 350ms poll, claims timed-out turns, 2-miss forfeit. |
| `sessionPersistence.js` | localStorage key `spine.activeOnlineSession`. |
| `spineMatchmaking.js` | Matchmaking flow orchestration. |
| `mockFirebase.js` | In-memory Firebase mock for unit tests. |
| `*.test.js` | Online service unit tests. |

### `src/game/account/` — User Features
| File | Role |
|------|------|
| `profileService.js` | User profile (name, avatar, stats). |
| `friendsService.js` | Friends list management. |
| `ratingService.js` | ELO rating. K=24, SCALE=400. `applyEloForFinishedGame()`. `RATINGS_LIMIT=10`. |
| `dictionaryService.js` | Word suggestion submission + admin moderation (requires `admin` custom claim). |
| `*.test.js` | Account service tests. |

### `src/game/settings/`
| File | Role |
|------|------|
| `settingsCompat.js` | Bridges legacy `bonusGameSettingsV1` localStorage format with spine settings. Defaults: `confirm=true`, `timelimit=false`, `botTime=medium`, `movelimit=false`, `music=true`, `vibration=true`. |

### `src/ui/controllers/`
| File | Role |
|------|------|
| `gameController.js` | Pure view-model for game screen. Tracks placed tiles, swaps, lock inventory, score. |
| `gameFlowController.js` | Wires in-game overlay intents (pause/resume/resign/end). |
| `animationController.js` | Subscribes to bus events, emits animation directives. Timing constants inside. |
| `scoreBonusAnimation.js` | Bonus point visualization. |
| `bonusActivationController.js` | Bonus mini-game flow coordination. |
| `turnTimerController.js` | Turn countdown display. |
| `disconnectController.js` | Offline grace period handling. |
| `tutorialController.js` | Tutorial guidance overlay. |
| `*.test.js` | Controller unit tests. |

### `src/ui/screens/`
See `/partials/screens/` for corresponding HTML templates.

**Menu/Home:** `menuScreen.js`, `asyncHomeButton.js`
**Setup & Lobby:** `setupScreen.js`, `createRoomScreen.js`, `joinCodeScreen.js`, `onlineLobbyScreen.js`, `waitingRoomScreen.js`, `incomingInviteScreen.js`, `matchmakingOverlayScreen.js`, `asyncSessionListScreen.js`
**Gameplay:** `gameScreen.js`, `pauseScreen.js`, `resignConfirmScreen.js`, `backConfirmScreen.js`, `coinTossScreen.js`, `endGameScreen.js`, `disconnectScreen.js`, `bonusIntroScreen.js`, `bonusSpectatorScreen.js`, `boostVetoScreen.js`, `boostBadges.js`
**User Management:** `authScreens.js`, `profileScreen.js`, `avatarScreens.js`, `friendsScreen.js`, `championsScreen.js`, `statsScreen.js`, `dictionaryScreen.js`, `settingsScreen.js`
**Mini-Games:** `miniGames/bonusTimer.js`, `miniGames/unscrambleMiniGame.js`, `miniGames/fillMiddleMiniGame.js`, `miniGames/crosswordMiniGame.js`, `miniGames/crossingWordsMiniGame.js`, `miniGames/honeycombMiniGame.js`, `miniGames/wheelMiniGame.js`, `miniGames/wordSearchMiniGame.js`
**Utilities:** `screenTransitions.js`, `jokerPicker.js`

### `src/notifications/`
| File | Role |
|------|------|
| `notificationService.js` | OneSignal push gateway. `configure()`, `boot()`, `loginUser()`, `attachBusSubscriptions()`. |
| `pushPayloadBuilder.js` | Pure transform to OneSignal REST body. `KIND` enum. |
| `asyncTurnBanner.js` | In-app async turn toast (Hebrew text). |
| `browserNotificationFallback.js` | Web Notification API fallback when OneSignal unavailable. |
| `inAppNotificationService.js` | Toast interface with injected renderer. `TOAST_KIND`. |
| `*.test.js` | Notification unit tests. |

### `src/ui/` (top-level)
| File | Role |
|------|------|
| `audioService.js` | Background music control. |
| `feedbackService.js` | Haptic/visual feedback. |
| `domHelpers.js` | DOM utility functions. |
| `screenPartialManifest.js` | Screen partial registry. |
| `screenPartials.js` | Partial HTML loading system. |

### `src/util/`
| File | Role |
|------|------|
| `rng.js` | Seeded RNG. `createRng()`, `shuffle()`. Used by tileBag for deterministic shuffles. |

### `src/testing/`
| File | Role |
|------|------|
| `firebaseRules.test.js` | Firebase security rule validation tests. |
| `serviceWorkerRouting.test.js` | Service Worker route testing. |

---

## `/tests/` — Test Suites

### `tests/unit/` (Node.js `--test`)
| File | Coverage Area |
|------|--------------|
| `engine-parity.test.js` | Core engine vs. legacy parity (primary) |
| `engine-parity-highrisk.test.js` | High-risk parity scenarios |
| `engine-parity-low-gaps.test.js` | Lower-risk gap coverage |
| `engine-parity-async-resume.test.js` | Async session resume |
| `engine-parity-pending-recovery.test.js` | Pending-tile recovery |
| `engine-parity-live-watchdog.test.js` | Live timer watchdog |
| `engine-parity-end-game-progression.test.js` | Game-over progression |
| `engine-parity-leave-game.test.js` | Leave/disconnect |
| `engine-parity-invite-lifecycle.test.js` | Invite send/accept/reject |
| `engine-parity-scoring-animation.test.js` | Scoring animation timing |
| `engine-parity-browser-notification-fallback.test.js` | Browser notification fallback |
| `disconnect-leave-e2e.test.js` | Disconnect/leave E2E flows |
| `firebase-rules.test.js` | Firebase security rules |
| `manifest.test.js` | PWA manifest validation |
| `logo-markup.test.js` | Logo rendering |
| `shailta-keyboard-removal.test.js` | Hebrew keyboard cleanup |

### `tests/e2e/` (Playwright)
| File | Coverage |
|------|----------|
| `spine-boot.spec.js` | Spine module loads in browser |
| `menu-routing.spec.js` | Navigation menu routing |
| `non-menu-buttons.spec.js` | UI button functionality |

### `tests/emulator/` (Firebase Emulator)
| File | Coverage |
|------|----------|
| `baseline.test.mjs` | Basic rules validation |
| `rules-audit.test.mjs` | Comprehensive rule coverage |
| `async-reminder-rules.test.mjs` | Async reminder auth rules |
| `timer-rules.test.mjs` | Timed game rule validation |
| `setup.mjs` | Emulator initialization |
| `README.md` | Emulator test instructions |

### `tests/` root
| File | Coverage |
|------|----------|
| `online-turn-sync.spec.js` | Live online sync validation |

---

## `/partials/screens/` — HTML Screen Templates
~20 HTML partial files loaded dynamically at runtime. Key ones:
- `home.html` — Main menu
- `game.html` — Game board (contains `#game-grid`, `#brack`, score panels, action buttons)
- `setup.html` — Game mode selection
- `online-lobby.html`, `online-create-room.html` — Online setup
- `log-in-screen.html`, `sign-up-screen.html` — Auth
- `profile-screen.html` — User profile
- `bonus-intro-shown-before-every-interactive-boost-mini-game.html` — Boost intro overlay
- `boost-veto-notice.html` — Veto confirm
- `pause-overlay.html`, `back-confirm-overlay.html` — In-game overlays

---

## `/data/`
| File | Role |
|------|------|
| `dictionary.base.txt` | Hebrew word list (464 KB). Loaded by `hebrewDictionary.js` at boot. |

---

## `/scripts/` — Build Tools
| File | Role |
|------|------|
| `stamp-build.js` | Update build timestamp in `sw.js` + `index.html` (cache name, version meta, SW registration query param). |
| `export-dictionary-file.js` | Extract embedded dictionary from legacy `index.html` to `data/dictionary.base.txt`. |
| `add-dictionary-words.js` | Bulk import words to dictionary. |
| `engine-parity-inventory.js` | Analyze engine feature coverage. |

---

## `/docs/` — Existing Parity Documentation
| File | Role |
|------|------|
| `engine-parity-test-plan.md` | Test strategy for legacy→spine parity |
| `engine-parity-audit-report.md` | Coverage audit (150+ legacy items) |
| `engine-parity-coverage-matrix.md` | Feature-by-feature coverage |
| `engine-parity-missing-report.md` | Resolved known gaps |
| `legacy-behavior-inventory.md` | 272+ legacy functions cataloged |
| `legacy-behavior-map.md` | Legacy ↔ new behavior mapping |
| `legacy-gameplay-parity-gap-report.md` | Gameplay differences with risk ratings |
| `legacy-vs-new-gap-report.md` | Overall gap analysis |
| `parity-test-plan.md` | Testing approach |
| `intentional-change-register.md` | 8 deliberate deviations from legacy |
| `spine-smoke-checklist.md` | Manual QA checklist |

---

## `/memory/` — Product Documentation
| File | Role |
|------|------|
| `MEMORY.md` | Index of product memory files |
| `project_design_system.md` | Boost Premium UI design system (April 2026: dark navy, glossy buttons, icon badges) |

---

## `/android/` — Android TWA Wrapper
Android Gradle project wrapping the PWA as a Trusted Web Activity.
- `build.gradle`, `settings.gradle`, `gradle.properties` — Build config
- `app/build.gradle` — App module (Gradle 8.4)
- `app/proguard-rules.pro` — Code obfuscation
- `app/src/main/AndroidManifest.xml` — Android manifest
- `app/src/main/res/` — Resources: 80+ locale value folders, mipmap icons (mdpi→xxxhdpi)

---

## `/assets/`
| Path | Role |
|------|------|
| `assets/music/` | Background music files (URL referenced via `config.js`) |

---

## Config / Infrastructure Files
| File | Role |
|------|------|
| `.github/workflows/firebase-rules.yml` | CI: deploy DB rules to `boost-8ef11` on push to main |
| `.well-known/assetlinks.json` | Android app association for TWA |
| `.claude/settings.local.json` | Local Claude Code permissions (allows `node -e`) |
| `.gitignore` | Excludes: `node_modules/`, `config.js`, build artifacts, `.firebase/` |

---

## Entry Points Summary

| Entry Point | When Used |
|-------------|-----------|
| `index.html` | Browser loads PWA |
| `src/main.js` | Module system boots spine |
| `sw.js` | Browser service worker |
| `data/dictionary.base.txt` | Loaded at runtime by `hebrewDictionary.js` |
| `tests/unit/*.test.js` | `npm run test:unit` |
| `tests/emulator/*.test.mjs` | `npm run test:emulator` |
| `tests/e2e/*.spec.js` | `npm run test:e2e` |
