# Archaic / Aramaic / liturgical-only lemmas to exclude (one per line).
#
# HSpell carries many Talmudic, biblical-only, and Aramaic entries that are
# technically Hebrew but no native speaker would recognize as game words.
# This file is the *manual* override on top of HSpell's archaic tags — for
# entries HSpell incorrectly classifies as Modern Hebrew.
#
# A lemma here is excluded even if HSpell does not tag it archaic.
# Seed list is intentionally small; expand during native-speaker review (§2e).

# (To be populated during review.)
